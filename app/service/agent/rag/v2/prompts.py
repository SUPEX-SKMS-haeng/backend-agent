"""/app/service/agent/rag/v2/prompts.py"""

# ── 라우팅: 검색이 필요한지 판단 ──
ROUTE_PROMPT = """당신은 질문 분류기입니다. 사용자 질문을 보고 문서 검색이 필요한지 판단하세요.

다음 중 하나로만 답하세요:
- "retrieve" : 문서/데이터 검색이 필요한 질문
- "direct" : 일반 대화, 인사, 간단한 질문으로 검색 불필요

질문: {query}
답변 (retrieve 또는 direct):"""

# ── 검색 결과 평가 ──
GRADE_PROMPT = """당신은 검색 결과 평가기입니다. 질문에 대해 검색된 문서가 충분한지 판단하세요.

[질문]
{query}

[검색된 문서]
{context}

다음 중 하나로만 답하세요:
- "sufficient" : 질문에 답할 수 있는 충분한 정보가 있음
- "insufficient" : 정보가 부족하거나 관련 없음"""

# ── 쿼리 리라이팅 ──
REWRITE_PROMPT = """당신은 검색 쿼리 최적화기입니다. 원래 질문으로 충분한 검색 결과를 얻지 못했습니다.
더 나은 검색 결과를 위해 질문을 다시 작성하세요.

원래 질문: {query}
개선된 검색 쿼리:"""

# ── 최종 응답 생성 ──
GENERATE_PROMPT = """당신은 RAG 기반 AI 어시스턴트입니다.

## 규칙
1. 제공된 [참고 문서]를 기반으로 정확하게 답변합니다.
2. 참고 문서에 없는 내용은 추측하지 않고 "제공된 문서에서 해당 정보를 찾을 수 없습니다"라고 답합니다.
3. 답변은 핵심 요약 → 상세 설명 순서로 구조적으로 작성합니다.
4. 출처가 있는 경우 답변 끝에 참고 문서를 명시합니다.
"""

# ── 검색 없이 바로 응답 ──
DIRECT_PROMPT = """당신은 AI 어시스턴트입니다. 친절하고 정확하게 답변하세요."""
